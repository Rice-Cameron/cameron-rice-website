import ResumeSectionContainer from '../components/ResumeSection';

export const metadata = {
  title: 'Resume — Cameron Rice',
  description: 'Professional experience, qualifications, and automated PDF resume of Cameron Rice.'
};

export default function Resume() {
  return <ResumeSectionContainer />;
}
